#include <string>
#include <vector>
#include <algorithm>

using namespace std;

int solution(vector<int> people, int limit) {
    int answer = 0;
    int left = limit;

    sort(people.begin(), people.end());
    
    for (int i = 0;i < people.size();i++) {

    }

    return answer;
}